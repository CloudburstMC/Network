name: External signaling conformance
on:
  push:
    branches:
      - 'develop'
      - 'nethernet'
  pull_request:
    branches:
      - 'develop'
      - 'nethernet'
  workflow_dispatch:
permissions:
  contents: read
jobs:
  conformance:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
      - name: Verify canonical protocol fixtures
        run: node docs/external-signaling/fixtures.mjs
      - name: Validate transport and independent provider
        run: ./gradlew --max-workers=2 build :external-signaling:nativeAdmissionTest
      - name: Verify shared diagnostic admissions against Java output
        run: node docs/external-signaling/diagnostic-v1.fixtures.mjs --java=external-signaling/build/diagnostic-java-admissions.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: nxs-conformance
          path: |
            **/build/test-results/**
            **/build/reports/tests/**
